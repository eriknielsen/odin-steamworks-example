package steamworkstest

import "core:fmt"
/*
main :: proc() {
    hello:= "hello there"
    bytes:= transmute([]u8)hello
    length:= len(bytes)
    hello_again:= string(bytes)
    fmt.println(hello_again)


}
    */